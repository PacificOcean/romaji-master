# Claude Code 入門ガイド（デスクトップアプリ版）
## プログラミング初心者でも使える！AI コーディングアシスタントの始め方

---

## Claude Code とは？

Claude Code は、Anthropic が開発した **AI コーディングアシスタント**です。
チャット感覚で日本語を入力するだけで、AI がコードを読んだり書いたり、バグを直したりしてくれます。

「コードを書いてほしい」「このエラーの意味を教えて」「ファイルを整理して」
――そんな指示を自然な言葉で伝えるだけで動きます。

---

## 必要なもの

- **Windows / Mac のパソコン**
- **Claude のアカウント**（無料で作成可能）
- インターネット接続

プログラミングの知識は不要です。

---

## Step 1 — Claude のアカウントを作る

まだアカウントがない場合は作成します。

1. [claude.ai](https://claude.ai/) をブラウザで開く
2. 「Sign up」をクリック
3. メールアドレスまたは Google / GitHub アカウントで登録
4. メール認証を完了する

> すでにアカウントがある場合はこの手順はスキップしてください。

---

## Step 2 — デスクトップアプリをダウンロードする

1. [claude.ai/download](https://claude.ai/download) にアクセス
2. お使いの OS（Windows または Mac）用のインストーラーをダウンロード
3. ダウンロードしたファイルをダブルクリックしてインストール

インストールが完了すると、デスクトップやスタートメニューに Claude のアイコンが追加されます。

---

## Step 3 — アプリを起動してログインする

1. Claude のデスクトップアプリを起動
2. 「Sign in」をクリック
3. Step 1 で作成したアカウントでログイン

これだけで準備完了です。API キーの設定など、難しい作業は一切不要です。

---

## Step 4 — Claude Code を起動する

アプリを起動したら、**ターミナル（端末）画面**を開きます。

### Windows の場合
- アプリ内のターミナルアイコンをクリック、または
- スタートメニューから「ターミナル」や「コマンドプロンプト」を開いて `claude` と入力

### Mac の場合
- アプリ内のターミナルアイコンをクリック、または
- Spotlight（⌘ + Space）で「ターミナル」を検索して開き、`claude` と入力

起動すると、こんな画面が表示されます。

```
Welcome to Claude Code!
>
```

`>` の後ろにカーソルが点滅していれば準備完了です。

---

## Step 5 — 話しかけてみよう

日本語でそのまま入力して Enter を押すだけです。

### 試してみる例

**プロジェクトの説明を聞く**
```
> このフォルダにどんなファイルがあるか教えて
```

**コードを書いてもらう**
```
> 「Hello, World!」と表示する Python のコードを書いて
```

**エラーの意味を聞く**
```
> TypeError: cannot read properties of undefined というエラーが出た。どういう意味？
```

**ファイルを修正してもらう**
```
> index.html を開いて、タイトルを「マイサイト」に変えて
```

---

## Step 6 — 作業フォルダを指定する

Claude Code は「今いるフォルダ」を対象に作業します。
作業したいプロジェクトのフォルダに移動してから起動しましょう。

### フォルダの移動方法

```bash
cd デスクトップ/my-project
```

または、エクスプローラー（Finder）でフォルダを開いた状態で
右クリック →「ターミナルで開く」を使うと簡単です。

---

## よく使う操作まとめ

| やりたいこと | 入力例 |
|-------------|--------|
| ファイルの内容を確認する | `app.js の内容を説明して` |
| コードを書いてもらう | `ボタンをクリックするとアラートが出る HTML を作って` |
| バグを直してもらう | `このコードが動かない。原因を調べて直して` |
| コードを説明してもらう | `この関数が何をしているか教えて` |
| ファイルを整理する | `不要なコメントを削除してコードをきれいにして` |

---

## 画面内で使える便利コマンド

Claude Code の画面（`>` が出ている状態）で使えるコマンドです。

| コマンド | 説明 |
|---------|------|
| `/help` | 使い方のヘルプを表示 |
| `/clear` | 会話の履歴をリセット |
| `/cost` | 今のセッションの使用量を確認 |
| `Ctrl + C` | 実行中の処理を中断 |
| `exit` | Claude Code を終了 |

---

## 困ったときの Q&A

### Q. 日本語で入力してもいい？
**A. はい、大丈夫です。** 日本語で指示を出せば日本語で返答してくれます。

### Q. 勝手にファイルを書き換えられない？
**A. 変更前に確認を求めてきます。** ファイルの編集や削除など重要な操作の前には
「この変更を行いますか？」と聞いてくれるので、安心して使えます。

### Q. 無料で使えますか？
**A. 無料プランでも使えます。** ただし利用回数に制限があります。
より多く使いたい場合は有料プラン（Claude Pro 等）への移行を検討してください。

### Q. インターネットが必要？
**A. 必要です。** AI の処理はクラウド上で行われるため、常時接続が必要です。

---

## 次のステップ

Claude Code に慣れてきたら、こんなことにも挑戦してみましょう。

- **CLAUDE.md を作成する** — プロジェクトごとに AI への指示を事前に書いておける
- **Git と連携する** — コードの変更履歴を管理しながら作業できる
- **MCP サーバーを追加する** — ブラウザ操作やデータベース接続など機能を拡張できる

---

## まとめ

1. claude.ai でアカウントを作成
2. デスクトップアプリをインストール
3. ログインして起動
4. 作業フォルダに移動
5. 日本語で話しかけるだけ！

難しい設定は一切なし。まずは気軽に話しかけてみてください。

---

*Claude Code デスクトップアプリ版 — 初心者向け入門ガイド*
